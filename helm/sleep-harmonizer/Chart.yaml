apiVersion: v2
name: sleep-harmonizer
description: Sleep Data Harmonizer — Oura + Withings wearable ingestion API
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "~16.7.0"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
